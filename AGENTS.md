# 个人网站维护约定

## 文章写作

- 先交代具体问题、场景或例子，再引入术语。术语第一次出现时，用一句日常语言解释它解决什么问题。
- 避免使用先否定、再用“而是”转折的模板句。直接说明观察、原因和结论。
- 少用“本质”“核心”“真正”“显然”等概括词。需要强调时，补上可以核对的条件、过程或数据。
- 技术结论优先引用论文、官方文档和源码。论坛、Issue 与社交媒体适合提供案例或发现线索，不能单独支撑普遍结论。
- 数字必须附带上下文，包括模型、硬件、软件版本、输入长度、样本量或来源；缺少实验记录时明确标为个人观察。
- 每一项优化都讲清楚节省了什么、仍需付出什么、在什么条件下会失效。
- 抽象流程优先配逐步例子、表格或自绘 SVG。转载素材前核对授权、署名和可修改范围。
- 保留作者自己的推导过程和边界判断，不把资料拼接成结论清单。

## 页面与排版

- 正文保持窄栏阅读，但不要在 `.post-content` 内重复套用带左右留白的 `.container`。
- 表格、代码和宽图在 320px 手机宽度下不得撑宽页面；必要时让局部元素横向滚动，并保留明显的滚动范围。
- 标题按 `h1`、`h2`、`h3` 顺序递进，同级标题表达同一层级。
- 图片必须有准确的替代文本；自绘 SVG 还要包含 `title` 和 `desc`。
- 新文章发布时同步更新 `blog.html`、`blog/posts.json`、`sitemap.xml`，并检查首页是否需要入口。
- 正文小字和提示框颜色至少满足 WCAG AA 的普通文本对比度要求。

## 验证与维护边界

- 提交前运行 `node tools/validate-site.mjs`、`git diff --check`，并在桌面和 320px 手机视口检查新文与被修改的旧文。
- 修改历史文章时保留当时的项目背景。后续架构变化用带日期的更新说明补充，不把今天的状态伪装成当时的事实。
- 缺少日志或原始数据时，不补写具体成功率和延迟；可以保留原观察，同时明确它不是通用基准。
- 删除文章时同时清理文章文件、列表卡片、JSON 索引、站点地图和硬编码数量。
